# Legacy alias fallback freeze budget
# Task 495 baseline (2026-02-22)
# 목적: alias 경로 증가를 차단하고, retire 웨이브까지 count drift를 방지한다.

BUDGET_WAVE=W495

# "legacy-alias-fallback" source tag occurrences in v10/src
MOD_ALIAS_FALLBACK_SOURCE_TAG_MAX=5

# Alias source strings occurrences in v10/src
MOD_ALIAS_SOURCE_TOOLBAR_TO_MOD_MAX=3
MOD_ALIAS_SOURCE_MOD_TO_TOOLBAR_MAX=3

# Mapping constant definitions must stay single-source in legacyAlias.ts
MOD_ALIAS_MAPPING_CONST_MAX=1
