import type { ModPackageUIItemRule } from "../../../types";

export const OPTIONAL_STRING_KEYS: Array<keyof ModPackageUIItemRule> = [
  "commandId",
  "label",
  "icon",
  "title",
  "group",
  "when",
];
